<UserControl
    x:Class="Fluence.Wpf.Demo.Pages.GalleryDataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Fluence.Wpf.Controls;assembly=Fluence.Wpf"
    xmlns:uicore="clr-namespace:Fluence.Wpf;assembly=Fluence.Wpf"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">
    <ui:SmoothScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" HorizontalAlignment="Stretch">
            <TextBlock
                Margin="0,0,0,4"
                ui:TextBlockExtensions.Typography="Title"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="Data" />
            <TextBlock
                Margin="0,0,0,24"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Data controls present lists, empty states, and related content groups for scanning and selection." />

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="ListView" />
            <TextBlock Style="{StaticResource SectionDescriptionStyle}" Text="Use ListView for compact single-column collections and richer item layouts." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:ListView
                            Height="230"
                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1">
                            <ListViewItem Content="Ana Bowman" />
                            <ListViewItem Content="Shawn Hughes" />
                            <ListViewItem Content="Oscar Ward" />
                            <ListViewItem Content="Madison Butler" />
                            <ListViewItem Content="Graham Barnes" />
                        </ui:ListView>
                        <ui:ListView
                            Grid.Column="2"
                            Height="230"
                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1">
                            <ListViewItem>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:FontIcon
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE77B;"
                                        IconFontSize="20" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="Ana Bowman" />
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="Support Engineer" />
                                    </StackPanel>
                                </Grid>
                            </ListViewItem>
                            <ListViewItem>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:FontIcon
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE77B;"
                                        IconFontSize="20" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="Shawn Hughes" />
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="Platform Specialist" />
                                    </StackPanel>
                                </Grid>
                            </ListViewItem>
                            <ListViewItem>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:FontIcon
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE77B;"
                                        IconFontSize="20" />
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="Oscar Ward" />
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="DevOps Lead" />
                                    </StackPanel>
                                </Grid>
                            </ListViewItem>
                        </ui:ListView>
                    </Grid>
                    <ui:HyperlinkButton
                        x:Name="ListViewItemsSourceLink"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="ListView empty state" />
            <TextBlock Style="{StaticResource SectionDescriptionStyle}" Text="Set EmptyContent for an intentional no-data state and animate removals when items leave the list." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <ui:ListView
                        x:Name="EmptyStateListView"
                        Height="180"
                        Margin="0,0,0,12"
                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                        <ui:ListView.EmptyContent>
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="No items. Add one to begin." />
                        </ui:ListView.EmptyContent>
                    </ui:ListView>
                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,8,0"
                            Appearance="Accent"
                            Click="AddListItem_Click"
                            Content="Add item" />
                        <ui:Button Click="RemoveListItem_Click" Content="Remove item" />
                    </StackPanel>
                    <ui:HyperlinkButton
                        x:Name="ListViewEmptyStateSourceLink"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Card variants" />
            <TextBlock Style="{StaticResource SectionDescriptionStyle}" Text="Use Card variants to group related data with different levels of visual emphasis." />
            <UniformGrid Margin="0,0,0,12" Columns="2">
                <ui:Card
                    MinHeight="110"
                    Margin="0,0,16,16"
                    Padding="18"
                    Header="Default"
                    Variant="{x:Static uicore:CardVariant.Default}">
                    <TextBlock Text="Standard surface for grouped content." TextWrapping="Wrap" />
                </ui:Card>
                <ui:Card
                    MinHeight="110"
                    Margin="0,0,0,16"
                    Padding="18"
                    Header="Outlined"
                    Variant="{x:Static uicore:CardVariant.Outlined}">
                    <TextBlock Text="Emphasizes the boundary over fill." TextWrapping="Wrap" />
                </ui:Card>
                <ui:Card
                    MinHeight="110"
                    Margin="0,0,16,0"
                    Padding="18"
                    Header="Filled"
                    Variant="{x:Static uicore:CardVariant.Filled}">
                    <TextBlock Text="Adds stronger container presence." TextWrapping="Wrap" />
                </ui:Card>
                <ui:Card
                    MinHeight="110"
                    Padding="18"
                    Header="Subtle"
                    Variant="{x:Static uicore:CardVariant.Subtle}">
                    <TextBlock Text="Keeps low-emphasis supporting content grouped." TextWrapping="Wrap" />
                </ui:Card>
            </UniformGrid>
            <ui:HyperlinkButton
                x:Name="CardVariantsSourceLink"
                Margin="0,0,0,16"
                HorizontalAlignment="Right"
                Style="{StaticResource DemoSourceLinkButtonStyle}"
                Content="Source" />
        </StackPanel>
    </ui:SmoothScrollViewer>
</UserControl>
