# Hugo build output (only the top-level public/, not template public/ folders).
/public/
resources/_gen/
.hugo_build.lock

# Hugo modules cache
_vendor/

# DocFX build output (merged into public/ at deploy time)
docfx/_site/
docfx/api/
docfx/obj/

# Local dev artifacts
.tmp/
