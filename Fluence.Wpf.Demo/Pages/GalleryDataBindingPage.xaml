<UserControl
    x:Class="Fluence.Wpf.Demo.Pages.GalleryDataBindingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Fluence.Wpf.Controls;assembly=Fluence.Wpf"
    xmlns:uicore="clr-namespace:Fluence.Wpf;assembly=Fluence.Wpf"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">
    <ui:SmoothScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" HorizontalAlignment="Stretch">
            <TextBlock
                Margin="0,0,0,4"
                ui:TextBlockExtensions.Typography="Title"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="Data Binding" />
            <TextBlock
                Margin="0,0,0,24"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="These examples keep the demo in code-behind while showing common WPF binding patterns used by Fluence.Wpf controls." />

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="ListView bound to ObservableCollection" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Add or remove items at runtime. The list updates through INotifyCollectionChanged." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="220" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBox
                            x:Name="NewItemBox"
                            Grid.Column="0"
                            KeyDown="NewItemBox_KeyDown"
                            PlaceholderText="New item name..." />
                        <ui:Button
                            Grid.Column="2"
                            Appearance="Accent"
                            Click="AddItem_Click"
                            Content="Add" />
                        <ui:Button
                            Grid.Column="4"
                            Click="RemoveItem_Click"
                            Content="Remove selected" />
                    </Grid>

                    <ui:ListView
                        x:Name="BoundListView"
                        Grid.Row="2"
                        SelectionMode="Single">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:FontIcon
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE8A5;"
                                        IconFontSize="16" />
                                    <StackPanel
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            ui:TextBlockExtensions.Typography="Body"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="{Binding Name}" />
                                        <TextBlock
                                            Margin="8,0,0,0"
                                            VerticalAlignment="Center"
                                            ui:TextBlockExtensions.Typography="Caption"
                                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                            Text="{Binding AddedAt}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>

                    <StackPanel Grid.Row="4">
                        <TextBlock
                            x:Name="ItemCountLabel"
                            ui:TextBlockExtensions.Typography="Caption"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="0 items" />
                        <ui:HyperlinkButton
                            x:Name="ObservableCollectionListViewSourceLink"
                            Margin="0,8,0,0"
                            HorizontalAlignment="Right"
                            Style="{StaticResource DemoSourceLinkButtonStyle}"
                            Content="Source" />
                    </StackPanel>
                </Grid>
            </ui:Card>

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="ListView SelectionMode" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Switch between Single, Multiple, and Extended selection modes." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <StackPanel Margin="0,0,0,12" Orientation="Horizontal">
                        <ui:RadioButton
                            x:Name="SingleModeRadio"
                            Margin="0,0,16,0"
                            Checked="SelectionMode_Changed"
                            Content="Single"
                            GroupName="SelectionModeGroup"
                            IsChecked="True" />
                        <ui:RadioButton
                            x:Name="MultipleModeRadio"
                            Margin="0,0,16,0"
                            Checked="SelectionMode_Changed"
                            Content="Multiple"
                            GroupName="SelectionModeGroup" />
                        <ui:RadioButton
                            x:Name="ExtendedModeRadio"
                            Checked="SelectionMode_Changed"
                            Content="Extended (Shift+Click)"
                            GroupName="SelectionModeGroup" />
                    </StackPanel>
                    <ui:ListView
                        x:Name="SelectionModeListView"
                        Height="200"
                        SelectionChanged="SelectionModeListView_SelectionChanged"
                        SelectionMode="Single">
                        <ListViewItem Content="Alpha" />
                        <ListViewItem Content="Bravo" />
                        <ListViewItem Content="Charlie" />
                        <ListViewItem Content="Delta" />
                        <ListViewItem Content="Echo" />
                        <ListViewItem Content="Foxtrot" />
                        <ListViewItem Content="Golf" />
                        <ListViewItem Content="Hotel" />
                    </ui:ListView>
                    <TextBlock
                        x:Name="SelectionCountLabel"
                        Margin="0,8,0,0"
                        ui:TextBlockExtensions.Typography="Caption"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="Selected: none" />
                    <ui:HyperlinkButton
                        x:Name="ListViewSelectionModeSourceLink"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="DataTemplate row" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Use an inline DataTemplate when every item shares the same row shape." />
            <ui:Card
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <ui:ListView
                        x:Name="DataTemplateListView"
                        Height="180"
                        SelectionMode="Single">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ui:FontIcon
                                        Grid.Column="0"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE8A5;"
                                        IconFontSize="16" />
                                    <StackPanel
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            ui:TextBlockExtensions.Typography="Body"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="{Binding Name}" />
                                        <TextBlock
                                            Margin="8,0,0,0"
                                            VerticalAlignment="Center"
                                            ui:TextBlockExtensions.Typography="Caption"
                                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                            Text="{Binding AddedAt}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>
                    <TextBlock
                        Margin="0,8,0,0"
                        ui:TextBlockExtensions.Typography="Caption"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="Name and AddedAt are simple properties on each bound item." />
                    <ui:HyperlinkButton
                        x:Name="DataTemplateRowSourceLink"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ui:SmoothScrollViewer>
</UserControl>
